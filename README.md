# Open-source version of preference elicitation module for react

When making changes:

- update `package.json` with new tag
- build using `yarn build-dev`
- git tag
- push and push tags
- update incoming tag in client project
